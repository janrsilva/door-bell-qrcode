/**
 * Application constants
 */

// Doorbell visit expiration time in milliseconds
export const DOORBELL_VISIT_EXPIRY_TIME_MS = 15 * 60 * 1000; // 15 minutes

// Doorbell visit expiration time in minutes (for display purposes)
export const DOORBELL_VISIT_EXPIRY_TIME_MINUTES = 15;
